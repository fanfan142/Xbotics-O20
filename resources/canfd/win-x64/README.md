# Windows CANFD runtime

This directory contains the 64-bit Windows CANFD runtime used by the desktop console.

The desktop console loads this bundled runtime on Windows for direct USB-CANFD control. Use an environment override only when you need another build.
